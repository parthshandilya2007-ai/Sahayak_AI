{
  "buildCommand": "cd frontend_template && npm install && npm run build",
  "outputDirectory": "frontend_template/.next",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}