{
  "installCommand": "cd frontend_template && npm install --legacy-peer-deps",
  "buildCommand": "cd frontend_template && npm run build",
  "outputDirectory": "frontend_template/.next",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
